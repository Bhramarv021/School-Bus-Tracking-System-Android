<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.schoolbustrackingsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SchoolBusTrackingSystem">
        <activity android:name=".viewmodule.ViewBusAttendantActivity"></activity>
        <activity android:name=".addmodule.AddBusAttendantActivity" />
        <activity android:name=".viewmodule.ViewStudentActivity" />
        <activity android:name=".addmodule.AddStudentActivity" />
        <activity android:name=".viewmodule.ViewBusActivity" />
        <activity android:name=".addmodule.AddBusActivity" />
        <activity android:name=".BusAttendantActivity" />
        <activity android:name=".ParentActivity" />
        <activity android:name=".SchoolHomeActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>